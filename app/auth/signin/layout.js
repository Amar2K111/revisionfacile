export const metadata = {
  title: "Connexion — Révision facile",
  description: "Connecte-toi pour générer tes fiches de révision.",
};

export default function SignInLayout({ children }) {
  return children;
}
